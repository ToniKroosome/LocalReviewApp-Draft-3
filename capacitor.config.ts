import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.localreviewapp',
  appName: 'LocalReviewApp',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
